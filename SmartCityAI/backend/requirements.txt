# SmartCity AI — Backend Dependencies
# 
# For basic server (no ML models):
#   No extra packages needed — uses Python stdlib only
#
# For advanced scikit-learn ML (optional upgrade):
#   pip install -r requirements.txt

numpy>=1.24.0
scikit-learn>=1.3.0
pandas>=2.0.0
